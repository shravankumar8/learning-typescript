/*Arrays and Loops:
Create an array of numbers. Use a loop to iterate through the array and log each element to the console. */

let numbers: number[] = [6, 54356, 67, 6, 5, 56, 45, 667];
for (let i: number = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
